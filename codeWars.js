function isIsogram(str){
    //...
    let word = str.toLowerCase();
    let testWord = str.toLowerCase();
    for (let i= 1; i<word.length; i++) {
        for (let k=1; k<testWord.length; k++) {
            
        }
    }
    console.log(word);
    console.log(testWord);
    
    
  };

  console.log(isIsogram('Csaba'));